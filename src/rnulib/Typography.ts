import {Typography} from 'react-native-ui-lib';

Typography.loadTypographies({
  title: {
    fontSize: 24,
    fontWeight: '500',
    letterSpacing: -0.6,
    textAlign: 'center',
  },
  linkRef: {
    fontSize: 18,
    fontWeight: '600',
    letterSpacing: -0.45,
  },
});
